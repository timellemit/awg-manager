// Package types holds the raw NDMS RCI response shapes — used by callers
// that need to parse JSON directly (e.g. legacy paths during migration).
// For cleaned-up domain types (e.g. ndms.Interface, ndms.Peer), import
// the parent internal/ndms package instead.
package types
